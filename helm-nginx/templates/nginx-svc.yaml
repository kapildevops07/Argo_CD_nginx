apiVersion: v1
kind: Service
metadata:
  name: backstage
  labels:
    app: backstage
spec:
  ports:
  - port: {{ .Values.backstage_port.http }}
    targetPort: {{ .Values.backstage_port.http }}
  selector:
    app: backstage